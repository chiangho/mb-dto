1、三个子项目提供查询接口。
  接口对外暴露，供主服务调用。接口为异步接口。在接受到主服务的请求后，将请求压入队列，
 子项目启动消费线程消费队列，获取捞取结果后，调用主服务接口更新图书信息。
 子项目目前暂时不涉及数据库操作。
 
2、如有涉及数据操作的，会开放各自的数据账号密码给各位。各位在自己的数据中操作。不可操作其他数据库。

3、dto各位在自己的分支上修改，如有要合并mater的，通知我合并。我会在合并后发布版本到maven私有库
  